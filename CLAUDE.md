# Client — Personal Coach

## Definition of done

A change is only done when all three are true:

- `npm test` — full suite passes
- `npm run lint` — no lint errors (pre-existing warnings are acceptable; do not introduce new ones)
- `npm run typecheck` — no type errors
